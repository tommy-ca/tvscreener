# .env.example - Environment variables template
# Create your local `.env` like:
#   cp .env.example .env
# `.env` is gitignored.

# Workflow defaults (recommended for Prefect server parity)
# Start a local server with:
#   prefect server start --host 127.0.0.1 --port 4200 --background
# Then runs use the dedicated server (no ephemeral startup) via PREFECT_API_URL.
PREFECT_HOME=.prefect-home
PREFECT_API_URL=http://127.0.0.1:4200/api

# Repo defaults for Prefect helper scripts (`workflows/prefect/config.py`).
TVSCREENER_PREFECT_HOST=127.0.0.1
TVSCREENER_PREFECT_PORT=4200
TVSCREENER_PREFECT_WORK_POOL=tvscreener
TVSCREENER_PREFECT_WORK_QUEUE=default
TVSCREENER_PREFECT_DATA_QUEUE=data
TVSCREENER_PREFECT_ANALYTICS_QUEUE=analytics

# Prefect deployment defaults (worker job env)
# For reproducibility, analytics/both deployments can set these env vars automatically.
# Set runtime to `auto` to omit it and let the code auto-select.
TVSCREENER_PREFECT_ANALYTICS_SEMANTIC_RUNTIME=sidemantic
TVSCREENER_PREFECT_ANALYTICS_PUBLISH_TABLE_ARTIFACTS=1
TVSCREENER_PREFECT_ANALYTICS_PUBLISH_RESULTS_SUMMARY=1

# Keep this for first-run ephemeral server migrations (if you *don't* set PREFECT_API_URL)
PREFECT_SERVER_EPHEMERAL_STARTUP_TIMEOUT_SECONDS=180

# Prefect task timeout (best-effort; blocking calls may not be interrupted)
TVSCREENER_PREFECT_TASK_TIMEOUT_SECONDS=1800

# Compose `pipeline_mode=both` from Prefect stage tasks
TVSCREENER_PREFECT_COMPOSE_BOTH=0

# Strategy filters
# TVSCREENER_MIN_VOLUME=500000
# TVSCREENER_MAX_ATR=2.0
# TVSCREENER_MIN_MA_RATING=0.5
# TVSCREENER_MIN_CONFLUENCE=1
# TVSCREENER_TREND_THRESHOLD=0.0
# TVSCREENER_MR_THRESHOLD=0.2
# TVSCREENER_MIN_ROC=

# Global defaults
# For the all-universe forex matrix rerun workflow:
TVSCREENER_DEFAULT_UNIVERSE=all
TVSCREENER_DEFAULT_TIMEFRAMES=15,60,240
TVSCREENER_CONTRACT_TYPE=cfd

# Lakehouse table naming layout:
# - legacy: shared `tvscreener.bronze|silver|gold` tables
# - scalable: per-asset/instrument/stage namespaces (e.g. `tvscreener_crypto_spot_bronze.screener_snapshot`)
TVSCREENER_LAKEHOUSE_LAYOUT=legacy

# Extensions lakehouse defaults (Track B)
TVSCREENER_LAKEHOUSE_BASE_DIR=.tvscreener/lakehouse
TVSCREENER_LAKEHOUSE_CATALOG_MODE=local
TVSCREENER_LAKEHOUSE_CATALOG_LOCAL_CATALOG_DB=catalog.db
TVSCREENER_LAKEHOUSE_CATALOG_LOCAL_WAREHOUSE_DIR=warehouse

# For remote mode (optional):
# TVSCREENER_LAKEHOUSE_CATALOG_MODE=remote
# TVSCREENER_LAKEHOUSE_CATALOG_REMOTE_URI=postgresql://user:pass@host:5432/db
# TVSCREENER_LAKEHOUSE_CATALOG_REMOTE_WAREHOUSE=s3://bucket/prefix
